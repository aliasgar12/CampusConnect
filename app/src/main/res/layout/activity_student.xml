<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="campusconnect.alias.com.campusconnect.ui.StudentActivity">

    <include
        android:id="@+id/toolbar"
        layout="@layout/toolbar"
        />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/toolbar"
        android:id="@+id/name_students"
        android:text="Students Completed"
        android:textSize="40sp"
        android:gravity="center"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"/>

    <android.support.v7.widget.RecyclerView
        android:id="@+id/list_student"
        android:layout_below="@+id/name_students"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingBottom="16dp"
        android:paddingLeft="16dp"
        android:paddingRight="16dp"
        android:paddingTop="16dp"
        tools:context=".ui.StudentActivity" />

</RelativeLayout>